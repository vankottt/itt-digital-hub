The AI Act agent reads the existing kit at `src/content/ai-act-kit`.

That directory remains the only copy of the legal sources, `SYSTEM_PROMPT.md`, and `AGENT_CONFIG.md`. This folder marks the agent knowledge root. It does not replace or update those files.
