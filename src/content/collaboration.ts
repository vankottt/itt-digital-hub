import type { CollaborationRoute } from "./types";

/** Unused academic collaboration catalogue. Public ITT contact is the enquiry form and published phone numbers. */

export const collaborationRoutes: CollaborationRoute[] = [];
